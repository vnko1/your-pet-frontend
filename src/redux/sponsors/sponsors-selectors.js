export const selectSponsors = (state) => state.sponsors.items;

export const selectSponsorsIsLoading = (state) => state.sponsors.isLoading;

export const selectSponsorsError = (state) => state.sponsors.error;
